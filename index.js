import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("root"));
const App = () => {
  return (
    <div>
      <h1>Hello, React App!</h1>
    </div>
  );
};

root.render(<App />);

/*** 
  
Made By: Abhishek Kumar
Github: https://github.com/abhiraj-ku
Note: Don't copy and publish by your name

Description : You don't have to manually configure react to write react code.
for example you don't have to type npm install react react-dom and parcel
this package does this job for you so that you can concentrate on coding only
Just type npm i react-parcel-config


****/
